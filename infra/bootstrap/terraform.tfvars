# This account already had a GitHub OIDC provider when the stack was first applied,
# created in July 2026 for an unrelated repository. There can be only one per account,
# so this stack adopts it. On a fresh account, delete this line.
create_oidc_provider = false
